---
// Navbar Flouvia Elite: Desktop Precision & Mobile Pill
---

<header class="navbar-wrapper" id="navbar">
    <div class="top-bar">
        <div class="container-fluid">
            <span>INGENIERÍA B2B & E-COMMERCE DE ALTO RENDIMIENTO</span>
            <span class="desktop-only">PRIVATE GROWTH BOUTIQUE</span>
        </div>
    </div>

    <nav class="main-nav">
        <div class="nav-container">
            
            <div class="nav-left desktop-nav">
                <div class="glass-pill">
                    <a href="/" class="pill-logo">FLOUVIA</a>
                    <a href="/" class="nav-link">TRABAJO</a>
                    <a href="/servicios" class="nav-link">SERVICIOS</a>
                    <a href="/nosotros" class="nav-link">NOSOTROS</a>
                    <a href="/casos" class="nav-link">CASOS</a>
                </div>
            </div>

            <div class="nav-center desktop-nav">
                <a href="/" class="main-logo" id="main-logo">FLOUVIA</a>
            </div>

            <div class="nav-right desktop-nav">
                <div class="mini-pill">
                    <span class="lang-text">ES</span>
                </div>

                <a href="/login" class="mini-pill user-link" aria-label="Login">
                    <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                        <circle cx="12" cy="7" r="4"></circle>
                    </svg>
                </a>
                
                <a href="#contacto" class="btn-contact">
                    CONTACTO 
                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <line x1="7" y1="17" x2="17" y2="7"></line>
                        <polyline points="7 7 17 7 17 17"></polyline>
                    </svg>
                </a>
            </div>

            <div class="mobile-pill-nav">
                <div class="mobile-pill-inner">
                    <button class="hamburger-btn" id="menu-toggle" aria-label="Abrir Menú">
                        <div class="burger-lines">
                            <span></span>
                            <span></span>
                            <span></span>
                        </div>
                    </button>

                    <a href="/" class="mobile-logo">FLOUVIA</a>

                    <a href="/login" class="mobile-action" aria-label="Portal de Clientes">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                            <circle cx="12" cy="7" r="4"></circle>
                        </svg>
                    </a>
                </div>
            </div>

        </div>
    </nav>

    <div class="mobile-menu-overlay" id="mobile-menu">
        <div class="mobile-menu-content">
            <nav class="mobile-links">
                <a href="/" class="m-link">Trabajo</a>
                <a href="/servicios" class="m-link">Servicios</a>
                <a href="/nosotros" class="m-link">Nosotros</a>
                <a href="/casos" class="m-link">Casos</a>
            </nav>
            
            <div class="mobile-menu-footer">
                <a href="#contacto" class="btn-luxe-mobile">Agendar Auditoría VIP</a>
                <div class="m-legal">
                    <span>CDMX — OP. GLOBAL</span>
                    <a href="mailto:flouvia.mx@gmail.com">flouvia.mx@gmail.com</a>
                </div>
            </div>
        </div>
    </div>

</header>

<style>
    :root {
        --color-azul-pro: #0a192f;
        --color-azul-dark: #061121; /* Azul aún más profundo para el menú */
        --ease-out-expo: cubic-bezier(0.19, 1, 0.22, 1);
    }

    /* 1. TOP BAR */
    .top-bar {
        background: var(--color-azul-pro); color: #fff;
        font-size: 9px; font-weight: 700; letter-spacing: 2px;
        padding: 9px 0; text-align: center; text-transform: uppercase;
    }
    .container-fluid { display: flex; justify-content: space-between; padding: 0 40px; max-width: 100%; align-items: center; }

    /* 2. WRAPPER */
    .navbar-wrapper { position: fixed; top: 0; left: 0; width: 100%; z-index: 1000; background: transparent; transition: background 0.3s ease;}
    .main-nav { padding: 25px 0; background: transparent; transition: padding 0.5s var(--ease-out-expo); }
    .navbar-wrapper.scrolled .main-nav { padding: 15px 0; }
    .nav-container { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; padding: 0 40px; position: relative;}

    /* =========================================
       ESCRITORIO (Desktop Nav)
       ========================================= */
    .desktop-nav { display: flex; align-items: center; }
    
    /* IZQUIERDA: Píldora */
    .glass-pill { display: inline-flex; align-items: center; background: rgba(0, 0, 0, 0.04); padding: 4px 6px; border-radius: 100px; border: 1px solid rgba(0, 0, 0, 0.05); transition: all 0.5s var(--ease-out-expo); }
    .navbar-wrapper.scrolled .glass-pill { background: rgba(10, 25, 47, 0.85); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); border: 1px solid rgba(255, 255, 255, 0.1); padding: 4px 12px; }

    .nav-link { color: #000; text-decoration: none; font-size: 9.5px; font-weight: 700; padding: 8px 14px; letter-spacing: 1px; opacity: 0.6; position: relative; z-index: 1; transition: all 0.3s ease; }
    .navbar-wrapper.scrolled .nav-link { color: #fff; }
    .nav-link::before { content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.06); border-radius: 100px; z-index: -1; opacity: 0; transform: scale(0.8); transition: all 0.3s var(--ease-out-expo); }
    .navbar-wrapper.scrolled .nav-link::before { background: rgba(255, 255, 255, 0.15); }
    .nav-link:hover { opacity: 1; }
    .nav-link:hover::before { opacity: 1; transform: scale(1); }

    .pill-logo { color: #fff; text-decoration: none; font-weight: 800; font-size: 13px; letter-spacing: 1px; width: 0; overflow: hidden; opacity: 0; transition: all 0.5s var(--ease-out-expo); white-space: nowrap;}
    .navbar-wrapper.scrolled .pill-logo { width: 75px; opacity: 1; margin-right: 10px; padding-left: 8px; }

    /* CENTRO: Logo Principal */
    .nav-center { justify-content: center; }
    .main-logo { color: #000; text-decoration: none; font-size: 24px; font-weight: 800; letter-spacing: 4px; transition: all 0.5s var(--ease-out-expo); }
    .navbar-wrapper.scrolled .main-logo { opacity: 0; transform: translateY(-10px); }

    /* DERECHA: Mini Píldoras */
    .nav-right { justify-content: flex-end; gap: 10px; }
    .mini-pill { display: flex; align-items: center; justify-content: center; background: rgba(0, 0, 0, 0.03); border: 1px solid rgba(0, 0, 0, 0.08); color: #000; border-radius: 100px; padding: 8px 14px; transition: all 0.3s ease; }
    .navbar-wrapper.scrolled .mini-pill { background: rgba(10, 25, 47, 0.85); border-color: rgba(255, 255, 255, 0.1); color: #fff; }
    
    .user-link { text-decoration: none; cursor: pointer; }
    .user-link:hover { background: rgba(0, 0, 0, 0.08); transform: translateY(-1px); }
    .navbar-wrapper.scrolled .user-link:hover { background: #fff; color: var(--color-azul-pro); border-color: #fff; box-shadow: 0 4px 15px rgba(255, 255, 255, 0.2); }
    .lang-text { font-size: 10px; font-weight: 800; letter-spacing: 1px; }

    .btn-contact { background: var(--color-azul-pro); color: #fff; padding: 10px 22px; border-radius: 100px; text-decoration: none; font-size: 10px; font-weight: 700; letter-spacing: 0.5px; display: flex; align-items: center; gap: 8px; transition: all 0.3s ease; border: 1px solid rgba(255,255,255,0.1); margin-left: 5px; }
    .btn-contact:hover { transform: translateY(-2px); box-shadow: 0 4px 15px rgba(10, 25, 47, 0.2); }

    /* Ocultar Píldora Móvil en Escritorio */
    .mobile-pill-nav { display: none; }


    /* =========================================
       MÓVIL (Mobile Pill & Menu)
       ========================================= */
    @media (max-width: 1024px) {
        .desktop-nav { display: none; }
        .desktop-only { display: none; }
        .container-fluid { justify-content: center; } /* Centra el texto de la top bar */
        .nav-container { display: flex; justify-content: center; padding: 0 20px; }
        .main-nav { padding: 15px 0; }

        /* LA PÍLDORA FLOTANTE (Estilo Getmore/Apple) */
        .mobile-pill-nav {
            display: flex;
            width: 100%;
            justify-content: center;
            position: relative;
            z-index: 2000; /* Siempre encima del menú */
        }

        .mobile-pill-inner {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: rgba(255, 255, 255, 0.85); /* Fondo glassmorphism claro */
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(0, 0, 0, 0.08);
            border-radius: 100px;
            padding: 8px 12px;
            width: 90%; /* Ocupa casi todo el ancho */
            max-width: 400px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.06);
            transition: all 0.4s var(--ease-out-expo);
        }

        /* Cuando scrolleas, se vuelve oscura (Azul Flouvia) */
        .navbar-wrapper.scrolled .mobile-pill-inner {
            background: rgba(10, 25, 47, 0.95);
            border-color: rgba(255, 255, 255, 0.1);
            box-shadow: 0 10px 30px rgba(10, 25, 47, 0.2);
        }

        /* Hamburgesa */
        .hamburger-btn {
            background: transparent;
            border: none;
            padding: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .burger-lines {
            width: 20px; height: 14px;
            position: relative;
            display: flex; flex-direction: column; justify-content: space-between;
        }
        .burger-lines span {
            display: block; width: 100%; height: 2px;
            background-color: var(--color-azul-pro);
            border-radius: 2px;
            transition: all 0.3s ease;
            transform-origin: left center;
        }
        .navbar-wrapper.scrolled .burger-lines span { background-color: #fff; }

        /* Animación Hamburgesa -> X (Cuando el menú está abierto) */
        .navbar-wrapper.menu-open .mobile-pill-inner {
            background: rgba(255, 255, 255, 0.1); /* Se vuelve translúcida sobre el fondo azul */
            border-color: rgba(255, 255, 255, 0.2);
        }
        .navbar-wrapper.menu-open .burger-lines span { background-color: #fff; }
        .navbar-wrapper.menu-open .burger-lines span:nth-child(1) { transform: rotate(45deg) translate(1px, -1px); width: 22px;}
        .navbar-wrapper.menu-open .burger-lines span:nth-child(2) { opacity: 0; width: 0;}
        .navbar-wrapper.menu-open .burger-lines span:nth-child(3) { transform: rotate(-45deg) translate(0px, 2px); width: 22px;}

        /* Logo Central en la Píldora */
        .mobile-logo {
            font-size: 14px;
            font-weight: 800;
            letter-spacing: 2px;
            color: var(--color-azul-pro);
            text-decoration: none;
            transition: color 0.4s ease;
        }
        .navbar-wrapper.scrolled .mobile-logo { color: #fff; }
        .navbar-wrapper.menu-open .mobile-logo { color: #fff; }

        /* Ícono Derecho (Login/User) */
        .mobile-action {
            display: flex; align-items: center; justify-content: center;
            width: 34px; height: 34px;
            background: rgba(0,0,0,0.04);
            border-radius: 50%; color: var(--color-azul-pro);
            transition: all 0.4s ease;
        }
        .navbar-wrapper.scrolled .mobile-action { background: rgba(255,255,255,0.1); color: #fff; }
        .navbar-wrapper.menu-open .mobile-action { background: rgba(255,255,255,0.1); color: #fff; }

        /* OVERLAY DEL MENÚ FULL SCREEN */
        .mobile-menu-overlay {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background-color: var(--color-azul-dark); /* Azul oscuro de lujo */
            z-index: 1500; /* Debajo de la píldora, encima del resto */
            display: flex;
            flex-direction: column;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            clip-path: circle(0% at 50% 10%); /* Animación tipo Apple de expansión circular */
            transition: all 0.6s var(--ease-out-expo);
        }

        .navbar-wrapper.menu-open .mobile-menu-overlay {
            opacity: 1;
            visibility: visible;
            clip-path: circle(150% at 50% 10%);
        }

        .mobile-menu-content {
            padding: 0 10%;
            display: flex;
            flex-direction: column;
            height: 70%;
            justify-content: space-between;
        }

        /* Links Gigantes */
        .mobile-links {
            display: flex; flex-direction: column; gap: 2rem;
            margin-top: 4rem; /* Espacio para que la píldora no tape */
        }
        .m-link {
            font-size: 3rem;
            font-weight: 800;
            letter-spacing: -1.5px;
            color: #fff;
            text-decoration: none;
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.5s var(--ease-out-expo);
        }
        /* Efecto escalonado al abrir */
        .navbar-wrapper.menu-open .m-link { opacity: 1; transform: translateY(0); }
        .navbar-wrapper.menu-open .m-link:nth-child(1) { transition-delay: 0.2s; }
        .navbar-wrapper.menu-open .m-link:nth-child(2) { transition-delay: 0.3s; }
        .navbar-wrapper.menu-open .m-link:nth-child(3) { transition-delay: 0.4s; }
        .navbar-wrapper.menu-open .m-link:nth-child(4) { transition-delay: 0.5s; }

        /* Footer del Menú Móvil */
        .mobile-menu-footer {
            display: flex; flex-direction: column; gap: 2rem;
            border-top: 1px solid rgba(255,255,255,0.1);
            padding-top: 2rem;
            opacity: 0; transform: translateY(20px);
            transition: all 0.5s var(--ease-out-expo) 0.6s; /* Entra al final */
        }
        .navbar-wrapper.menu-open .mobile-menu-footer { opacity: 1; transform: translateY(0); }

        .btn-luxe-mobile {
            background: #fff; color: var(--color-azul-pro);
            padding: 16px 0; border-radius: 100px;
            text-align: center; text-decoration: none;
            font-size: 1.1rem; font-weight: 700;
            width: 100%;
        }

        .m-legal { display: flex; justify-content: space-between; font-size: 0.8rem; font-weight: 600; color: #8892b0; letter-spacing: 1px; }
        .m-legal a { color: #8892b0; text-decoration: none; }
    }
</style>

<script>
    const navbar = document.getElementById("navbar");
    const menuToggle = document.getElementById("menu-toggle");

    // Lógica de Scroll (Scrolled State)
    window.addEventListener("scroll", () => {
        let scrollTop = window.pageYOffset || document.documentElement.scrollTop;
        if (scrollTop > 60) {
            navbar.classList.add("scrolled");
        } else {
            navbar.classList.remove("scrolled");
        }
    });

    // Lógica del Menú Móvil (Open/Close)
    if(menuToggle) {
        menuToggle.addEventListener("click", () => {
            navbar.classList.toggle("menu-open");
            
            // Prevenir el scroll del body cuando el menú está abierto
            if(navbar.classList.contains("menu-open")) {
                document.body.style.overflow = "hidden";
            } else {
                document.body.style.overflow = "";
            }
        });
    }

    // Cerrar menú al hacer clic en un link (para que navegue correctamente)
    const mobileLinks = document.querySelectorAll('.m-link, .btn-luxe-mobile');
    mobileLinks.forEach(link => {
        link.addEventListener('click', () => {
            navbar.classList.remove("menu-open");
            document.body.style.overflow = "";
        });
    });
</script>