<section id="casos" class="section-cases container">
    <div class="reveal">
        <p class="eyebrow-cases">Casos de Éxito</p>
        <h2 class="section-title">Ingeniería que Convierte Marcas.</h2>
    </div>

    <div class="grid-cases">
        <div class="case-card reveal">
            <div class="icon-box">B2B</div>
            <div class="case-content">
                <h4>Maritima Global: Supply Chain Optimization.</h4>
                <p>Descubre cómo digitalizamos la operación mayorista B2B, reduciendo la fricción de pedidos un 40%.</p>
                <div class="case-metrics">
                    <span class="metric">EFICIENCIA +40%</span>
                </div>
                <a href="/casos/maritima" class="case-link">Leer caso privado →</a>
            </div>
        </div>
        
        <div class="case-card reveal">
            <div class="icon-box">AOV</div>
            <div class="case-content">
                <h4>Fine Jewelry: Retención de Clientes de Alto Valor.</h4>
                <p>Digitalizamos la experiencia de compra de joyería fina para B2C, incrementando el AOV promedio un 25%.</p>
                <div class="case-metrics">
                    <span class="metric">TICKET PROM. +25%</span>
                </div>
                <a href="/casos/joyeria" class="case-link">Leer caso privado →</a>
            </div>
        </div>
    </div>
</section>

<style>
    .section-cases {
        padding: 8rem 0;
        position: relative;
    }
    .eyebrow-cases {
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 3px;
        font-weight: 800;
        color: #888;
        margin-bottom: 1.5rem;
        text-align: center;
    }
    .section-title {
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        font-weight: 800;
        margin-bottom: 5rem;
        text-align: center;
        letter-spacing: -1.5px;
        color: #000;
    }
    .grid-cases {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 2.5rem;
    }
    .case-card {
        background: #ffffff;
        border: 1px solid rgba(0, 0, 0, 0.08);
        border-radius: 12px;
        padding: 3rem 2.5rem;
        transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
        box-shadow: 0 4px 20px rgba(0,0,0,0.02);
    }
    .case-card:hover {
        transform: translateY(-5px);
        border-color: #000;
        box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    }
    .icon-box {
        font-size: 0.9rem;
        font-weight: 800;
        color: #000;
        background: #f5f5f5;
        width: 60px;
        height: 60px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 12px;
        border: 1px solid rgba(0, 0, 0, 0.05);
        margin-bottom: 2rem;
    }
    .case-content h4 {
        color: #000;
        font-size: 1.3rem;
        margin-bottom: 1rem;
        font-weight: 700;
        letter-spacing: -0.5px;
        line-height: 1.3;
    }
    .case-content p {
        color: #555;
        font-size: 0.95rem;
        margin-bottom: 1.5rem;
        line-height: 1.6;
    }
    .case-metrics {
        margin-bottom: 2rem;
    }
    .metric {
        display: inline-block;
        font-weight: 800;
        color: #000;
        background: rgba(0, 0, 0, 0.05);
        padding: 0.4rem 1rem;
        border-radius: 6px;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }
    .case-link {
        color: #000;
        font-weight: 700;
        text-decoration: none;
        font-size: 0.9rem;
        transition: color 0.3s ease;
        border-bottom: 1px solid #000;
        padding-bottom: 2px;
    }
    .case-link:hover {
        color: #666;
        border-bottom-color: #666;
    }
</style>