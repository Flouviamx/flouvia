<section class="hero-premium">
    <div class="container">
        <div class="hero-content reveal">
            <p class="eyebrow">ESTUDIO DE INGENIERÍA DIGITAL</p>
            <h1>Sistemas de conversión <br> <span class="italic">para marcas</span> líderes.</h1>
            <p class="description">
                Diseñamos y desarrollamos arquitecturas digitales de alto rendimiento. 
                De Shopify Plus a estrategias de Paid Media: transformamos tráfico en capital.
            </p>
            <div class="hero-stats">
                <div class="stat-item">
                    <span class="label">ENFOQUE</span>
                    <span class="value">ROI / DATA-DRIVEN</span>
                </div>
                <div class="stat-item">
                    <span class="label">ESPECIALIDAD</span>
                    <span class="value">E-COMMERCE & B2B</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .hero-premium {
        padding: 14rem 0 8rem 0;
        background: #fcfcfc;
        min-height: 90vh;
        display: flex;
        align-items: center;
    }
    .container { max-width: 1300px; margin: 0 auto; padding: 0 5%; }
    .eyebrow {
        font-size: 10px;
        font-weight: 800;
        letter-spacing: 4px;
        color: #888;
        margin-bottom: 2rem;
    }
    h1 {
        font-size: clamp(3rem, 7vw, 5.5rem);
        line-height: 0.95;
        font-weight: 800;
        letter-spacing: -3px;
        color: #000;
        margin-bottom: 3rem;
    }
    .italic { font-style: italic; font-weight: 400; font-family: serif; color: #333; }
    .description {
        font-size: 1.25rem;
        color: #555;
        max-width: 550px;
        line-height: 1.6;
        margin-bottom: 4rem;
    }
    .hero-stats {
        display: flex;
        gap: 4rem;
        border-top: 1px solid rgba(0,0,0,0.08);
        padding-top: 3rem;
    }
    .stat-item { display: flex; flex-direction: column; gap: 8px; }
    .label { font-size: 9px; font-weight: 800; color: #aaa; letter-spacing: 2px; }
    .value { font-size: 12px; font-weight: 700; color: #000; }
</style>