---
// src/pages/en/portal/boveda.astro
export const prerender = false;
import PortalLayout from '../../../layouts/PortalLayout.astro';
import BovedaUI from '../../../components/portal/BovedaUI.astro';

const { userId } = await Astro.locals.auth();
if (!userId) return Astro.redirect('https://os.flouvia.com/en/login');
---

<PortalLayout title="Vault | Flouvia OS">
    <BovedaUI lang="en" />
</PortalLayout>